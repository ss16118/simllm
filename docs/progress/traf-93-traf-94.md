# TRAF-93 and TRAF-94 progress

This document is the working ledger for the Simple buffered-read implementation
and the standardized NCCL primitive experiment. It records what is implemented,
what was verified, and what remains open. Hardware results are not claimed until
the corresponding frozen campaign is complete and its guards pass.

## Scope and order

1. **TRAF-93:** implement `buffered_read` for Simple on the retained GPU/NVLink
   calendar while preserving the existing LL, LL128 and source-off paths.
2. **TRAF-94:** implement the manifest-driven primitive runner, capability and
   source-conformance diagnostics, raw result schema, and analysis gates.
3. Run hardware measurements only after the runner inventory and capability
   outcomes have been frozen as required by the existing v1 experiment design.

The normative inputs are:

- `examples/nccl_simple_read_v1/expectations.md` and `expectations.json`;
- `docs/design/nccl-primitive-identification-v1.md`;
- `examples/nccl_primitive_identification_v1/manifest.json`.

## TRAF-93 checklist

- [x] Read the frozen expectations and inspect the retained packet and NCCL
  runtime boundaries.
- [x] Confirm that component packetization already represents a peer read as a
  zero-payload request followed by payload-bearing response packets.
- [x] Admit peer reads on the retained packet calendar without weakening the
  existing peer-write preflight.
- [x] Add a one-shot response gate so owner-memory service becomes eligible
  only after request visibility and responses become eligible only after that
  service completes.
- [x] Extend `NcclRingProgram` and `NcclExecutionConfig` with explicit
  `buffered_read`, applying it only to Simple payload movement.
- [x] Emit read-specific source bindings that preserve requester, buffer owner,
  connection sequence, stripe, request and response identity.
- [x] Preserve empty-slice control work without inventing payload reads.
- [x] Reject placement changes on an existing communicator and unsupported
  registered/direct/proxy branches before admission.
- [x] Add exact isolated-delay, memory-rate, FIFO-reuse, source-bypass and
  collective/graph tests from the frozen expectations.
- [x] Add and run the `examples/nccl_simple_read_v1` mechanism study.

## TRAF-94 checklist

- [x] Define the expanded-cell representation and deterministic manifest digest.
- [x] Implement sequential stage expansion rather than a blind Cartesian
  product.
- [x] Implement required result-row validation and explicit qualification
  reasons.
- [x] Implement already-ready, delayed-publication, delayed-consumption,
  empty/nonempty, copy/reduction and working-set primitive probes.
- [x] Add channel/warp/SM resource-sharing probes after the one-channel pilot.
- [x] Keep diagnostic source-operation counts separate from ordinary timing in
  the runner/probe contract, and emit them only from diagnostic native runs.
- [x] Capture requested and realized controls, source/build/device identity,
  local timer boundaries, correctness and process exits.
- [x] Implement five-process completeness, paired-IQR resolution and held-out
  prediction checks.
- [x] Freeze the capability-qualified expanded H100 inventory before ordinary
  timing. H100 is an extension; it is not silently labeled A100 or GH200.
- [x] Run and publish the hardware campaign, including immutable negative
  results when the frozen accuracy rule is not met.

## Verification log

| Date | Branch | Evidence | Result |
|---|---|---|---|
| 2026-09-11 | `traf-93-simple-read` | Repository and frozen-design inspection | Packetizer read support exists; retained read admission and source-memory response gating are missing. |
| 2026-09-11 | `traf-93-simple-read` | Focused retained packet/NCCL tests | 221 tests passed while the implementation was being integrated. |
| 2026-09-11 | `traf-93-simple-read` | Expanded packet/NCCL regression set | 368 passed; one assertion expected the old validation wording and was corrected without changing behavior. |
| 2026-09-11 | `traf-93-simple-read` | Final combined packet/NCCL regression (`PYTHONPATH=. .venv/bin/pytest -q ...`) | 370 passed in 14.89 seconds. |
| 2026-09-11 | `traf-93-simple-read` | `nccl_simple_read_v1/run_study.py` | All fatal guards valid: 9 isolated-read, 6 remote-memory, 288 collective and 32 graph-metric rows. |
| 2026-09-11 | `traf-93-simple-read` | Ruff over every changed Python file; `git diff --check` | Passed. |
| 2026-09-11 | `traf-94-primitive-measurements` | H100 planned expansion | 848 sequential timing cells and 32 source-specialization capability pilots; planned inventory digest `8dc3ac3ca36bfa42b70c52ea21d601fc88273d7a9b24b54de632321189bbb1b4`. |
| 2026-09-11 | `traf-94-primitive-measurements` | Local H100 environment audit | Not qualified: working NCCL checkout is at the wrong commit and dirty; installed NCCL 2.30.7 lacks a verified binding to the pinned 2.31.2 source. No timing launched. |
| 2026-09-11 | `traf-94-primitive-measurements` | Matrix, qualification, schedule, mock process, artifact collection, completeness, paired-IQR and holdout tests | 8 passed in 1.05 seconds. Mock evidence is test-only and carries no hardware claim. |
| 2026-09-11 | `traf-94-primitive-measurements` | Repository-wide pytest attempt | Interrupted during a long study section after 2,144 passed and 14 skipped. One failure was a generic `run_study` module-name collision introduced by the new test and is fixed; its exact regression test now passes. The other was an environment-dependent system-`pip` wheel build and passes when the virtual-environment `pip` is first on `PATH`. |
| 2026-09-11 | `traf-94-primitive-measurements` | Combined TRAF-93/TRAF-94 and discovered-regression set with venv `pip` on `PATH` | 380 passed in 17.41 seconds; Ruff and `git diff --check` passed. |
| 2026-09-11 | `traf-94-primitive-measurements` | Clean pinned NCCL build for `sm_90` | NCCL 2.31.2 built successfully from `7b83616df3ae082a1f32bb74c27458bfe8153a13`; library SHA-256 `57161bd381053afad3fab8a717caafe472e6dbbe24e751828930d281bd2f50b9`. |
| 2026-09-11 | `traf-94-primitive-measurements` | Build-record plus clean H100 environment audit | Capability-build gate passed; build-record digest `f801de5f55ef02167fe63816a8ae406fe5ca6f9309a3bb550d1ccab0b71aadd2`. This is not primitive capability or timing evidence. |
| 2026-09-16 | `traf-94-primitive-measurements` | Refreshed `upstream/main` and compared all TRAF-94 paths | Upstream has no newer probe or hardware campaign to reuse; the source-faithful executable remains the missing implementation. |
| 2026-09-16 | `traf-94-primitive-measurements` | `SOURCE_PROBE_DESIGN.md` | Froze the implementation mapping from every manifest family to an actual pinned-NCCL source site, including ordinary/diagnostic separation and fatal qualification guards. |
| 2026-09-16 | `traf-94-primitive-measurements` | Expanded-inventory type audit | Found and fixed the confirmation-stage default being emitted as a one-element JSON list instead of the scalar family `confirmation`; the H100 inventory must be regenerated and re-frozen before timing. |
| 2026-09-16 | `traf-94-primitive-measurements` | Capability-request audit | Added the deterministic representative cell controls to each grouped capability request; the earlier request named a source family but did not contain enough values to launch or verify a real device pilot. |
| 2026-09-16 | `traf-94-primitive-measurements` | Capability grouping audit | Replaced 32 overly broad family-only pilots with 388 support-sensitive pilots covering rank/channel/warp/SM geometry, FIFO reservations, empty work, and rotating allocation. The strongest delay/size member represents only support-equivalent timing cells. |
| 2026-09-16 | `traf-94-primitive-measurements` | Pinned NCCL source probe implementation | Added a versioned patch over NCCL `7b83616d`: LL/LL128/Simple source counters and interventions, explicit P2P protocol/warp controls, Simple buffered/DirectRead selection, same-device `%globaltimer` intervals, and realized protocol/channel/block/worker/placement/SM evidence. |
| 2026-09-16 | `traf-94-primitive-measurements` | Native harness and strict adapter | Added `primitive_probe.cu`, `build_probe.py`, and `probe.py`; the harness uses public NCCL P2P for readiness/reuse and segmented Ring AllReduce for data/sharing/confirmation, exportable `ncclMemAlloc` buffers, CUDA green contexts, three timer boundaries, and post-timing exact correctness. |
| 2026-09-16 | `traf-94-primitive-measurements` | H100 source-path smoke matrix | LL, LL128, Simple buffered and Simple DirectRead passed already-ready, delayed-publication, delayed-consumption, empty/nonempty, sum, 8/32-channel sharing, and observed-delay checks. A wider four-rank Simple DirectRead sum is correctly retained as unsupported because the forced pinned-source path produces an incorrect result; buffered Simple and LL four-rank confirmation pass. |
| 2026-09-16 | `traf-94-primitive-measurements` | Final instrumented NCCL/probe provenance | NCCL 2.31.2 `sm_90` library SHA-256 `fe444e4a9ebe18e99ba31e10bff50ee2b4ae1c3c7de3775b20baac484c43dae0`; instrumentation patch SHA-256 `c53bb397ad92f7bec46cc5b6d94c34070b51c87d2db865a8c1afc840c2d6547f`; build-record digest `3beb1d0935c9a685173fb398f097784c8111634f6abc21ce03c20fcf74b0ef12`; probe identity `8612974c171d7de4d39fbecef383d4ecf8402754c1a368d95fb911b5ad8423e8`; environment gate passed. |
| 2026-09-16 | `traf-94-primitive-measurements` | Full H100 capability sweep and inventory freeze | All 388 capability requests completed: 334 qualified and 54 explicitly rejected. The resulting 768-cell inventory digest is `a0915efc4442aa290aaf82f8de48b3327e9c62d3ff94d4fdd8563fbdb2eaefa7`; the exact hashes and rejected keys are in `h100-capability-freeze.json`. |
| 2026-09-16 | `traf-94-primitive-measurements` | Ordinary-path smoke work item | Work index 0 completed 20 warmups and 100 measured repetitions, emitting 300 qualified rows across the three required timers; observation SHA-256 `1940c6b100447ef3e0be8bdf9e7ce0e434ec7e79a3146e0e9fd8dc0b34141b72`. This proves the normal runner path, not completion of the 3,840-item campaign. |
| 2026-09-16 | `traf-94-primitive-measurements` | Focused TRAF-93/TRAF-94 regression suite | 242 passed in 10.57 seconds; Ruff and `git diff --check` passed. |
| 2026-09-17 | `traf-94-primitive-measurements` | Resumable ordinary-campaign supervisor | Added selected-physical-GPU UUID resolution, stable-idle gating, hash-verified resume, atomic status records, and strict serial execution so two probes cannot contend on the shared NVSwitch. Four focused supervisor tests plus the existing TRAF-94 tests pass: 13 passed in 1.39 seconds; Ruff and `git diff --check` passed. |
| 2026-09-17 | `traf-94-primitive-measurements` | In-progress frozen ordinary campaign | 467 of 3,840 schedule items have hash-complete work directories and zero have `FAILED.json`. The live supervisor is waiting at work index 467 because external jobs occupy the selected GPUs; it will resume only after the configured stable-idle boundary. This is progress evidence, not a parameter or completion claim. |
| 2026-09-17 | `traf-94-primitive-measurements` | Inner-timer fatal-guard audit at work boundary 467 | Found 700 rows across seven empty already-ready LL/LL128 cell scopes whose source-local start was absent, producing a node-uptime-sized interval near `1.78959e18` ns. Every other completed inner interval was contained by its matching CUDA event. The campaign was stopped safely, an exact-unit and causal-nesting guard was added, raw evidence was retained, and the affected complete `(cell, same_device_interval)` scopes are explicitly void rather than selectively dropped. The unchanged probe identity and frozen schedule then resumed from all 467 hash-verified work items. Fourteen focused tests, Ruff, and `git diff --check` pass. |
| 2026-09-17 | `traf-94-primitive-measurements` | Identification/confirmation analysis lock | Added a self-digested identification-only nonparametric component profile, explicit resolved-versus-joint contrast ledger, conservative anchor-IQR propagation, source-constrained Ring transfer, and a separately invoked held-out scorer that rejects another fit, inventory, or observation hash. The design and equation are in `ANALYSIS_DESIGN.md`. A compact synthetic end-to-end case proves that confirmation rows do not enter the fit and that a resolved held-out delta is scored only after the lock. Thirty focused/documentation tests, Ruff, and `git diff --check` pass. |
| 2026-09-17 | `traf-94-primitive-measurements` | External-contention boundary audit | The existing supervisor established stable idle before every launch and queried selected GPUs again at the next work boundary. Parsing that evidence found 16 completed jobs followed by a newly visible external context; overlap is not established, but isolation is not provable for those jobs. Added an explicit immediate post-run check and `CONTAMINATED.json` rejection without placing an NVML observer inside timed work. The 16 conservative-risk jobs and the original pre-campaign smoke item are retained in external quarantine and rerun under the strengthened bracket. |
| 2026-09-17 | `traf-94-primitive-measurements` | Compact result publication path | Added `publish_results.py` to verify and cross-bind the merged-artifact, validation, fit, score, inventory, observation, and self-digest identities. It publishes only the load-bearing parameter surfaces plus grouped separability and held-out outcomes, while leaving bulk rows and the full per-process fit external. A mutation test proves that another observation hash is rejected. Thirty-one focused/documentation tests, Ruff, and `git diff --check` pass. |
| 2026-09-17 | `traf-94-primitive-measurements` | Broader NCCL/NVLink/NVSwitch regression | The explicit integration set covering primitive identification, channel FIFO/steps, NCCL stack and demo, DGX NVLink, htsim NVLink, native NVSwitch, and documentation completed with 337 passed in 87.89 seconds. |
| 2026-09-17 | `traf-94-primitive-measurements` | Campaign resume on idle device set | After the conservative contention quarantine, 450 of 3,840 work items remain hash-verified and accepted, with no `FAILED.json` or `CONTAMINATED.json` markers. The supervisor was stopped while waiting—not while timing—on devices 0,2,4,5 because external jobs remained resident on 0 and 2. The unchanged frozen schedule and probe identity resumed on idle devices 1,4,5,7, retaining the three-sample pre-run idle gate, serial execution, artifact-hash resume checks, and immediate post-run context check. A first resume command omitted `TRAF94_PROBE_CONFIG` and exited during probe-identity setup before timing or writing an artifact; the corrected launch uses `/tmp/traf94-probe-final5/probe-build.json` and began rerunning quarantined work index 0. The host-local `campaign-launch.json` records the exact UUID transition. |
| 2026-09-17 | `traf-94-primitive-measurements` | Post-run contention guard triggered as designed | Eleven quarantined indices were cleanly rerun, bringing the accepted count to 461. Immediately after work index 429 exited, the post-run check observed a transient external `python3` context on selected GPU 7 and stopped with the dedicated contamination status. The complete attempt and marker are preserved under `quarantine/postcheck-device-transition-20260917/work-000429-attempt2`; they are excluded from collection, and active work contains zero failure or contamination markers before the required rerun. |
| 2026-09-17 | `traf-94-primitive-measurements` | Second post-run contention rejection | Work index 429 and then 436 were accepted cleanly, raising progress to 463. The guard rejected the following work-439 attempt after external `python3` PID 3530096 appeared on selected GPU 7 with approximately 19 GiB resident; another external context occupied GPU 6, leaving only three GPUs idle. The attempt is preserved as `quarantine/postcheck-device-transition-20260917/work-000439-attempt2`, active work is clean, and the serial supervisor waits rather than weakening the four-GPU isolation rule. |
| 2026-09-18 | `traf-94-primitive-measurements` | Repeated work-439 contention rejection | A second rerun of work index 439 was conservatively rejected when transient external `python3` PID 3641802 appeared on selected GPU 7 with 548 MiB at the immediate post-run boundary. The unchanged accepted count is 463; attempt 3 and its marker are preserved beside attempt 2 in the host quarantine. At the next host snapshot, devices 1,4,5,7 were all clear, allowing a new three-sample-gated rerun without changing the frozen schedule or probe. |
| 2026-09-18 | `traf-94-primitive-measurements` | 500-work milestone | Work-439 attempt 4 passed both contention boundaries, the remaining quarantined indices were recovered, and the campaign advanced into new schedule work. The authoritative active-work count reached 501 of 3,840 hash-complete attempts with zero `FAILED.json` and zero `CONTAMINATED.json` markers; prior rejected attempts remain preserved outside active work. The supervisor continues serially on devices 1,4,5,7 toward the next 100-work reporting boundary. |
| 2026-09-18 | `traf-94-primitive-measurements` | 600-work milestone | The authoritative active-work count reached 601 of 3,840 hash-complete attempts. The 500-to-600 interval completed without a failure or contention stop; active work contains zero `FAILED.json` and zero `CONTAMINATED.json` markers, and the serial supervisor continues on devices 1,4,5,7. |
| 2026-09-18 | `traf-94-primitive-measurements` | 700-work milestone | The authoritative active-work count reached 701 of 3,840 hash-complete attempts. The 600-to-700 interval completed without a failure or contention stop; active work contains zero `FAILED.json` and zero `CONTAMINATED.json` markers, and the serial supervisor continues on devices 1,4,5,7. |
| 2026-09-18 | `traf-94-primitive-measurements` | 800-work milestone | The authoritative active-work count reached 803 of 3,840 hash-complete attempts before the checkpoint read. The 700-to-800 interval completed without a failure or contention stop; active work contains zero `FAILED.json` and zero `CONTAMINATED.json` markers, and the serial supervisor continues on devices 1,4,5,7. |
| 2026-09-18 | `traf-94-primitive-measurements` | Work-823 contention rejection and device transition | The accepted count reached 823 before the post-run guard observed external `.conda/bin/python` PID 4163811 on selected GPU 1 immediately after work 823. That attempt and marker are preserved under `quarantine/postcheck-device-transition-20260918/work-000823-attempt1` and excluded from active work. Because devices 4,5,6,7 were all idle, the unchanged frozen schedule and probe moved to that quartet with the same three-sample pre-run gate, serial execution, hash checks, and immediate post-run guard. |
| 2026-09-18 | `traf-94-primitive-measurements` | 900-work milestone | Work index 823 reran cleanly on devices 4,5,6,7, and the authoritative active-work count reached exactly 900 of 3,840 hash-complete attempts. Active work contains zero `FAILED.json` and zero `CONTAMINATED.json` markers; the serial supervisor continues on the same quartet. |
| 2026-09-18 | `traf-94-primitive-measurements` | 1,000-work milestone | The authoritative active-work count reached 1,003 of 3,840 hash-complete attempts before the checkpoint read. The 900-to-1,000 interval completed without a failure or contention stop; active work contains zero `FAILED.json` and zero `CONTAMINATED.json` markers, and the serial supervisor continues on devices 4,5,6,7. |
| 2026-09-18 | `traf-94-primitive-measurements` | Work-1057 contention rejection and device transition | The accepted count reached 1,057 before the post-run guard observed external `tsc_owner` PID 41898 using 7,750 MiB on selected GPU 4 immediately after work 1,057. That attempt and marker are preserved under `quarantine/postcheck-device-transition-20260918/work-001057-attempt1` and excluded from active work. Because devices 0,1,5,6 were idle, the unchanged frozen schedule and probe moved to that quartet with the same three-sample pre-run gate, serial execution, hash checks, and immediate post-run guard. |
| 2026-09-18 | `traf-94-primitive-measurements` | 1,100-work milestone | Work index 1,057 reran cleanly on devices 0,1,5,6, and the authoritative active-work count reached exactly 1,100 of 3,840 hash-complete attempts. Active work contains zero `FAILED.json` and zero `CONTAMINATED.json` markers; the serial supervisor continues on the same quartet. |
| 2026-09-18 | `traf-94-primitive-measurements` | 1,200-work milestone | The authoritative active-work count reached 1,201 of 3,840 hash-complete attempts before the checkpoint read. Between work items, the supervisor correctly waited through short-lived Abaqus contexts on devices 0/1 and `tree_search` contexts on device 6, restarting the three-sample idle gate each time; no measurement launched during those occupied windows. Active work contains zero `FAILED.json` and zero `CONTAMINATED.json` markers. |
| 2026-09-18 | `traf-94-primitive-measurements` | 1,300-work milestone | The authoritative active-work count reached 1,302 of 3,840 hash-complete attempts before the checkpoint read. The 1,200-to-1,300 interval completed without a failure or contention stop; active work contains zero `FAILED.json` and zero `CONTAMINATED.json` markers, and the serial supervisor continues on devices 0,1,5,6. |
| 2026-09-18 | `traf-94-primitive-measurements` | 1,400-work milestone | The authoritative active-work count reached 1,402 of 3,840 hash-complete attempts before the checkpoint read. The 1,300-to-1,400 interval completed without a failure or contention stop; active work contains zero `FAILED.json` and zero `CONTAMINATED.json` markers, and the serial supervisor continues on devices 0,1,5,6. |
| 2026-09-18 | `traf-94-primitive-measurements` | Work-1468 contention rejection and device transition | The accepted count reached 1,468 before the post-run guard observed external `base_head` PID 154296 using 7,750 MiB on selected GPU 6 immediately after work 1,468. That attempt and marker are preserved under `quarantine/postcheck-device-transition-20260918/work-001468-attempt1` and excluded from active work. A successor `k2_mine` context remained on GPU 6, while devices 0,1,5,7 were idle, so the unchanged frozen schedule and probe moved to that quartet with all isolation and resume guards unchanged. |
| 2026-09-18 | `traf-94-primitive-measurements` | 1,500-work milestone | Work index 1,468 reran cleanly on devices 0,1,5,7, and the authoritative active-work count reached 1,502 of 3,840 hash-complete attempts before the checkpoint read. Active work contains zero `FAILED.json` and zero `CONTAMINATED.json` markers; the serial supervisor continues on the same quartet. |
| 2026-09-18 | `traf-94-primitive-measurements` | 1,600-work milestone | The authoritative active-work count reached exactly 1,600 of 3,840 hash-complete attempts. The 1,500-to-1,600 interval completed without a failure or contention stop; active work contains zero `FAILED.json` and zero `CONTAMINATED.json` markers, and the serial supervisor continues on devices 0,1,5,7. |
| 2026-09-18 | `traf-94-primitive-measurements` | 1,700-work milestone | The authoritative active-work count reached 1,701 of 3,840 hash-complete attempts before the checkpoint read. The 1,600-to-1,700 interval completed without a failure or contention stop; active work contains zero `FAILED.json` and zero `CONTAMINATED.json` markers, and the serial supervisor continues on devices 0,1,5,7. |
| 2026-09-18 | `traf-94-primitive-measurements` | 1,800-work milestone | The authoritative active-work count reached 1,802 of 3,840 hash-complete attempts before the checkpoint read. The 1,700-to-1,800 interval completed without a failure or contention stop; active work contains zero `FAILED.json` and zero `CONTAMINATED.json` markers, and the serial supervisor continues on devices 0,1,5,7. |
| 2026-09-18 | `traf-94-primitive-measurements` | 1,900-work milestone | The authoritative active-work count reached 1,904 of 3,840 hash-complete attempts before the checkpoint read. The 1,800-to-1,900 interval completed without a failure or contention stop; active work contains zero `FAILED.json` and zero `CONTAMINATED.json` markers, and the serial supervisor continues on devices 0,1,5,7. |
| 2026-09-18 | `traf-94-primitive-measurements` | 2,000-work milestone | The authoritative active-work count reached 2,003 of 3,840 hash-complete attempts before the checkpoint read. The 1,900-to-2,000 interval completed without a failure or contention stop; active work contains zero `FAILED.json` and zero `CONTAMINATED.json` markers, and the serial supervisor continues on devices 0,1,5,7. |
| 2026-09-18 | `traf-94-primitive-measurements` | Work-2076 contention rejection and device transition | The accepted count reached 2,076 before the post-run guard observed external `tsc_new` PID 339448 using 518 MiB on selected GPU 7 immediately after work 2,076. That attempt and marker are preserved under `quarantine/postcheck-device-transition-20260918/work-002076-attempt1` and excluded from active work. Because devices 0,1,4,5 were idle while devices 2 and 3 remained occupied, the unchanged frozen schedule and probe moved to 0,1,4,5 with all isolation and resume guards unchanged. |
| 2026-09-18 | `traf-94-primitive-measurements` | Work-2093 contention rejection | The accepted count reached 2,093 before the post-run guard observed transient external `tsc_new` PID 350991 using 7,750 MiB on selected GPU 4 immediately after work 2,093. That attempt and marker are preserved under `quarantine/postcheck-device-transition-20260918/work-002093-attempt1` and excluded from active work. Devices 2 and 3 remained occupied by external Python jobs and devices 6 and 7 by Abaqus, so the unchanged schedule will rerun index 2,093 on devices 0,1,4,5 only after the quartet passes the three-sample idle gate. |
| 2026-09-18 | `traf-94-primitive-measurements` | 2,100-work milestone | Work indices 2,076 and 2,093 reran cleanly after their rejected attempts were quarantined, and the authoritative active-work count reached 2,102 of 3,840 hash-complete attempts before the checkpoint read. Active work contains zero `FAILED.json` and zero `CONTAMINATED.json` markers. The supervisor continues on devices 0,1,4,5 with the same stable-idle and immediate post-run guards. |
| 2026-09-18 | `traf-94-primitive-measurements` | Work-2102 contention rejection and device transition | The accepted count remained 2,102 when the post-run guard observed a second transient `tsc_new` process on selected GPU 4, PID 363607 using 7,750 MiB, immediately after work 2,102. That attempt and marker are preserved under `quarantine/postcheck-device-transition-20260918/work-002102-attempt1` and excluded from active work. Because devices 0,1,5,6 were idle, the unchanged frozen schedule moved away from repeatedly targeted GPU 4 to that quartet with all guards unchanged. |
| 2026-09-18 | `traf-94-primitive-measurements` | Work-2118 contention rejection and device transition | The accepted count reached 2,118 before the post-run guard observed external `dem-expansion` Python PID 372089 using 522 MiB on selected GPU 0 immediately after work 2,118; it remained resident at the operator snapshot. That attempt and marker are preserved under `quarantine/postcheck-device-transition-20260918/work-002118-attempt1` and excluded from active work. Because devices 1,5,6,7 were idle, the unchanged frozen schedule moved to that quartet with all guards unchanged. |
| 2026-09-18 | `traf-94-primitive-measurements` | 2,200-work milestone | Work indices 2,102 and 2,118 reran cleanly after their rejected attempts were quarantined, and the authoritative active-work count reached 2,206 of 3,840 hash-complete attempts before the checkpoint read. Active work contains zero `FAILED.json` and zero `CONTAMINATED.json` markers. The supervisor continues on devices 1,5,6,7 with the same stable-idle and immediate post-run guards. |
| 2026-09-18 | `traf-94-primitive-measurements` | 2,300-work milestone | The authoritative active-work count reached 2,304 of 3,840 hash-complete attempts before the checkpoint read. The 2,200-to-2,300 interval completed without a failure or contention stop; active work contains zero `FAILED.json` and zero `CONTAMINATED.json` markers, and the serial supervisor continues on devices 1,5,6,7. |
| 2026-09-18 | `traf-94-primitive-measurements` | 2,400-work milestone | The authoritative active-work count reached 2,401 of 3,840 hash-complete attempts before the checkpoint read. The 2,300-to-2,400 interval completed without a failure or contention stop; active work contains zero `FAILED.json` and zero `CONTAMINATED.json` markers, and the serial supervisor continues on devices 1,5,6,7. |
| 2026-09-18 | `traf-94-primitive-measurements` | Work-2426 contention rejection and device transition | The accepted count reached 2,426 before the post-run guard observed external two-GPU `sync_bench` PID 512649 using 524 MiB on selected GPU 1 immediately after work 2,426; the process was visible on GPUs 0 and 1 at the operator snapshot. That attempt and marker are preserved under `quarantine/postcheck-device-transition-20260918/work-002426-attempt1` and excluded from active work. Because devices 4,5,6,7 were idle, the unchanged frozen schedule moved to that quartet with all guards unchanged. |
| 2026-09-18 | `traf-94-primitive-measurements` | 2,500-work milestone | Work index 2,426 reran cleanly on devices 4,5,6,7 after its rejected attempt was quarantined, and the authoritative active-work count reached 2,503 of 3,840 hash-complete attempts before the checkpoint read. Active work contains zero `FAILED.json` and zero `CONTAMINATED.json` markers. The supervisor continues on the same quartet with the stable-idle and immediate post-run guards unchanged. |
| 2026-09-18 | `traf-94-primitive-measurements` | Work-2542 contention rejection and device transition | The accepted count reached 2,542 before the post-run guard observed transient external `tsc_fix` PID 547589 using 14 MiB on selected GPU 6 immediately after work 2,542. That attempt and marker are preserved under `quarantine/postcheck-device-transition-20260918/work-002542-attempt1` and excluded from active work. Because devices 0,1,4,5 were idle, the unchanged frozen schedule moved to that quartet with all guards unchanged. |
| 2026-09-18 | `traf-94-primitive-measurements` | 2,600-work milestone | Work index 2,542 reran cleanly on devices 0,1,4,5 after its rejected attempt was quarantined, and the authoritative active-work count reached 2,602 of 3,840 hash-complete attempts before the checkpoint read. Active work contains zero `FAILED.json` and zero `CONTAMINATED.json` markers. The supervisor continues on the same quartet with the stable-idle and immediate post-run guards unchanged. |
| 2026-09-18 | `traf-94-primitive-measurements` | 2,700-work milestone | The authoritative active-work count reached 2,703 of 3,840 hash-complete attempts before the checkpoint read. The 2,600-to-2,700 interval completed without a failure or contention stop; active work contains zero `FAILED.json` and zero `CONTAMINATED.json` markers, and the serial supervisor continues on devices 0,1,4,5. |
| 2026-09-18 | `traf-94-primitive-measurements` | 2,800-work milestone | The authoritative active-work count reached 2,804 of 3,840 hash-complete attempts before the checkpoint read. The 2,700-to-2,800 interval completed without a failure or contention stop; active work contains zero `FAILED.json` and zero `CONTAMINATED.json` markers, and the serial supervisor continues on devices 0,1,4,5. |
| 2026-09-18 | `traf-94-primitive-measurements` | 2,900-work milestone | The authoritative active-work count reached 2,901 of 3,840 hash-complete attempts before the checkpoint read. The 2,800-to-2,900 interval completed without a failure or contention stop; active work contains zero `FAILED.json` and zero `CONTAMINATED.json` markers, and the serial supervisor continues on devices 0,1,4,5. |
| 2026-09-18 | `traf-94-primitive-measurements` | Work-2951 contention rejection and device transition | The accepted count reached 2,951 before the post-run guard observed two external `tree_search_cluster_ep8` contexts, PIDs 686715 and 687002 each using 7,750 MiB, on selected GPU 0 immediately after work 2,951; both remained resident at the operator snapshot. That attempt and marker are preserved under `quarantine/postcheck-device-transition-20260918/work-002951-attempt1` and excluded from active work. Because devices 1,5,6,7 were idle, the unchanged frozen schedule moved to that quartet with all guards unchanged. |
| 2026-09-18 | `traf-94-primitive-measurements` | 3,000-work milestone | Work index 2,951 reran cleanly on devices 1,5,6,7 after its rejected attempt was quarantined, and the authoritative active-work count reached 3,004 of 3,840 hash-complete attempts before the checkpoint read. Active work contains zero `FAILED.json` and zero `CONTAMINATED.json` markers. The supervisor continues on the same quartet with the stable-idle and immediate post-run guards unchanged. |
| 2026-09-18 | `traf-94-primitive-measurements` | Work-3076 contention rejection and device transition | The accepted count reached 3,076 before the post-run guard observed external `tree_search_cluster_ep8` PID 718436 using 7,750 MiB on selected GPU 6 immediately after work 3,076; at the operator snapshot that process and paired PID 718607 on GPU 7 remained resident. That attempt and marker are preserved under `quarantine/postcheck-device-transition-20260918/work-003076-attempt1` and excluded from active work. Because devices 0,1,4,5 were idle, the unchanged frozen schedule moved to that quartet with all guards unchanged. |
| 2026-09-18 | `traf-94-primitive-measurements` | 3,100-work milestone | Work index 3,076 reran cleanly on devices 0,1,4,5 after its rejected attempt was quarantined, and the authoritative active-work count reached 3,102 of 3,840 hash-complete attempts before the checkpoint read. Active work contains zero `FAILED.json` and zero `CONTAMINATED.json` markers. The supervisor continues on the same quartet with the stable-idle and immediate post-run guards unchanged. |
| 2026-09-18 | `traf-94-primitive-measurements` | 3,200-work milestone | The authoritative active-work count reached 3,203 of 3,840 hash-complete attempts before the checkpoint read. The 3,100-to-3,200 interval completed without a failure or contention stop; active work contains zero `FAILED.json` and zero `CONTAMINATED.json` markers, and the serial supervisor continues on devices 0,1,4,5. |
| 2026-09-18 | `traf-94-primitive-measurements` | 3,300-work milestone | The authoritative active-work count reached 3,303 of 3,840 hash-complete attempts before the checkpoint read. The 3,200-to-3,300 interval completed without a failure or contention stop; active work contains zero `FAILED.json` and zero `CONTAMINATED.json` markers, and the serial supervisor continues on devices 0,1,4,5. |
| 2026-09-18 | `traf-94-primitive-measurements` | 3,400-work milestone | The authoritative active-work count reached exactly 3,400 of 3,840 hash-complete attempts. The 3,300-to-3,400 interval completed without a failure or contention stop; active work contains zero `FAILED.json` and zero `CONTAMINATED.json` markers, and the serial supervisor continues on devices 0,1,4,5. |
| 2026-09-18 | `traf-94-primitive-measurements` | 3,500-work milestone | The authoritative active-work count reached 3,503 of 3,840 hash-complete attempts before the checkpoint read. The 3,400-to-3,500 interval completed without a failure or contention stop; active work contains zero `FAILED.json` and zero `CONTAMINATED.json` markers, and the serial supervisor continues on devices 0,1,4,5. |
| 2026-09-19 | `traf-94-primitive-measurements` | 3,600-work milestone | The authoritative campaign status and independent `COMPLETE.json` marker count both reached 3,603 of 3,840 hash-complete attempts before the checkpoint read. The 3,500-to-3,600 interval completed without a failure or contention stop; active work contains zero `FAILED.json` and zero `CONTAMINATED.json` markers, and the unchanged serial supervisor continues on devices 0,1,4,5. |
| 2026-09-19 | `traf-94-primitive-measurements` | 3,700-work milestone | The authoritative campaign status and independent `COMPLETE.json` marker count both reached 3,704 of 3,840 hash-complete attempts before the checkpoint read. The 3,600-to-3,700 interval completed without a failure or contention stop; active work contains zero `FAILED.json` and zero `CONTAMINATED.json` markers, and the unchanged serial supervisor continues on devices 0,1,4,5. |
| 2026-09-19 | `traf-94-primitive-measurements` | 3,800-work milestone | The authoritative campaign status and independent `COMPLETE.json` marker count both reached 3,801 of 3,840 hash-complete attempts before the checkpoint read. The 3,700-to-3,800 interval completed without a failure or contention stop; active work contains zero `FAILED.json` and zero `CONTAMINATED.json` markers, and the unchanged serial supervisor continues on devices 0,1,4,5 for the final 39 items. |
| 2026-09-19 | `traf-94-primitive-measurements` | Measurement campaign complete | The serial supervisor exited successfully after all 3,840 frozen work items. A terminal audit found exactly 3,840 active work directories and 3,840 hash-complete `COMPLETE.json` markers, with zero active `FAILED.json` and zero active `CONTAMINATED.json` markers. Rejected attempts remain preserved only in the documented quarantine directories; no quarantined attempt is part of the collected dataset. |
| 2026-09-19 | `traf-94-primitive-measurements` | Collection and validation complete | The hash-verifying collector accepted all 3,840 work items and merged 1,152,000 rows from 768 qualified cells. Validation reported zero failures and `valid_outside_void_scopes`; it retained all raw evidence while excluding eight complete `same_device_interval` scopes whose epoch-sized source-local value exceeded the enclosing CUDA event. The merged observation SHA-256 is `594769e4d2871f119a05f47dd5f8a60cc03e8bb3475766da2d8e1162f86ecb5d`. |
| 2026-09-19 | `traf-94-primitive-measurements` | Locked fit and held-out score complete | The identification-only fit used zero confirmation rows and locked digest `f4d53fa06fffc0576ce0751bd9af638b447f2ec3f4982e9b7034ac33bfc6fce1`: 1,792 anchors and 3,012 matched contrasts, of which 656 resolve separately and 2,356 remain joint intervals. The subsequently opened holdout contains 1,308 adjacent interventions; 441 resolve, 97 meet the frozen error bound, and 344 fail it. The result is honestly published as `FAIL` with digest `0ab17cae8b0390e1493bbd8636214a530fdf4ef8eeccd186b5f5dbd0ad5da069`; the v1 additive transfer candidate is not a calibrated channel model. |
| 2026-09-19 | `traf-94-primitive-measurements` | Analysis scalability correction | The full fit exposed repeated whole-campaign scans for every anchor and contrast. Added a documented one-pass `(cell, timer, process)` median index used by fit and score. Besides the focused indexed-versus-direct and synthetic lock/score tests, a full 1,152,000-row indexed rerun reproduced the original fit byte-for-byte at SHA-256 `3eb92f17f0d133e964d02e92d121f915861fe73a29b2161f8fd5129da04180a8` and fit digest `f4d53fa06fffc0576ce0751bd9af638b447f2ec3f4982e9b7034ac33bfc6fce1`. This changes analysis execution cost, not evidence, estimands, acceptance rules, or the locked H100 result. |
| 2026-09-19 | `traf-94-primitive-measurements` | Final regressions | The focused primitive-identification and documentation set completed with 31 passed in 2.17 seconds. The broader integration set covering primitive identification, channel FIFO/steps, NCCL stack and demo, DGX NVLink, htsim NVLink, native NVSwitch, and documentation completed with 337 passed in 71.77 seconds. Ruff over the changed Python files and `git diff --check` passed. |
| 2026-09-19 | `traf-94-v2-model` | V2 model and independent holdout freeze | Preserved the v1 `FAIL` and declared all 168 opened v1 confirmation cells as training evidence for a protocol/placement/timer-specific five-axis tensor model. The new 128-cell, 32-capability plan uses only pre-opened intermediate levels and freezes 112 in-range predictions; the remaining 16 are exactly rank-3 Simple buffered-read cells outside the qualified v1 rank plane and must be rejected by capability evidence or stop the campaign before timing. Manifest digest `2bf7455bf91bf6081b11ed82652e5258826cf59486c30e618cbcda2a7e662942`; planned-inventory digest `0495e9583c5692ab229f3aa3bef93951a96a1a06ee7ddf40cc2ff6ce1a983cef`; locked-model digest `fba72bb7fe28e112cf4f2b73ca7381d5ea9ec9c55e29a1f4d1a4f6cfb1e15c0e`. No v2 holdout value was observed or used during this fit. |
| 2026-09-19 | `traf-94-v2-model` | V2 preregistration reproducibility gate | A clean regeneration from the frozen v1 artifacts reproduced both `holdout-plan.json` and `h100-v2-model.json` byte-for-byte (file SHA-256 `306010b1241a3cd86c29651857bbc21409b77c48620a50eced4e457e11956696` and `c38242bd820dfb5c883882b0340dd544d36d42b88a9cc6b505437e449cdd1d21`). The combined v1/v2/documentation suite completed with 34 passed in 1.67 seconds; Ruff and `git diff --check` passed. This gate precedes every v2 capability and ordinary timing run. |
| 2026-09-19 | `traf-94-v2-model` | V2 capability gate and ordinary-run start | All 32 capability requests completed against pinned probe identity `8612974c171d7de4d39fbecef383d4ecf8402754c1a368d95fb911b5ad8423e8`: 28 qualified and exactly the four rank-3 Simple buffered-read keys failed the destination correctness check (`got 4, expected 6`). Those four keys exclude the 16 deliberately unpredicted cells. The resulting 112-cell qualified inventory has digest `1cb2137bfbe4dcb6043485a570bc6f54177a51c646946db3da4fbaebfeee70ec`, is an exact rectangular subset of the frozen plan, and is completely covered by pre-opened predictions. Its 560-work serial schedule has digest `033d8a3c06e699da2c6ddd8cbb03490a22ea04bc1ee448d59a98114697cc9800`; ordinary timing began only after these checks passed, on physical GPUs 0,1,2,5 with the unchanged idle and post-run guards. |
| 2026-09-19 | `traf-94-v2-model` | V2 100-work milestone | The authoritative status and independent `COMPLETE.json` count reached 103 of 560 hash-complete attempts at the checkpoint. Active work contains zero `FAILED.json` and zero `CONTAMINATED.json` markers; the frozen serial schedule continues on physical GPUs 0,1,2,5. |
| 2026-09-19 | `traf-94-v2-model` | V2 200-work milestone | The authoritative status and independent `COMPLETE.json` count reached 204 of 560 hash-complete attempts at the checkpoint. The 100-to-200 interval completed with zero `FAILED.json` and zero `CONTAMINATED.json` markers; the frozen serial schedule continues on physical GPUs 0,1,2,5. |
| 2026-09-19 | `traf-94-v2-model` | V2 300-work milestone | The authoritative status and independent `COMPLETE.json` count both reached exactly 300 of 560 hash-complete attempts. The 200-to-300 interval completed with zero `FAILED.json` and zero `CONTAMINATED.json` markers; the frozen serial schedule continues on physical GPUs 0,1,2,5. |
| 2026-09-19 | `traf-94-v2-model` | V2 execution, validation, and locked score complete | The unchanged supervisor completed all 560 work items; the final audit found 560 work directories and 560 `COMPLETE.json` markers with zero `FAILED.json` and zero `CONTAMINATED.json` markers. Collection produced 168,000 rows with SHA-256 `7c0299623ef0531f132fb5203ca97057dc3f6798fa094688aaab9a76f94e723b`; validation found zero failures and zero void scopes. Of 816 interventions, 225 resolve, 180 satisfy the frozen bound, and 45 do not. The immutable outcome is `FAIL`, score digest `e3375317f6cc83a8beee733467c352e3f1a9b46599042f64bd2cc8587adf1704`, compact result digest `380cb4e7c26b98cc66305b25094d7ce1fcdf3d49c69bb4b38bfe33040a5856b5`. |
| 2026-09-19 | `traf-94-v2-model` | V3 exact-table replication freeze | Preserved the v2 `FAIL` and narrowed the next claim to same-node, same-probe, exact-control repeatability. The v3 model is the median of five v2 process medians at each qualified control tuple and has no interpolation, extrapolation, or nearest-neighbour path. Before any v3 timing, the 128-cell plan and all 112 predictable entries were frozen: manifest digest `fcfb930b006d14860659e68e1917b141c2866c89e142a010fd86949611e788f1`, planned-inventory digest `40cb2875a3800fbfc116041a3601701d75cf5adeea04494b2df6de6d6e241bd6`, model digest `acd919dd3c1ee237f0e6f107b024fcc0396fa8710f2aace7261146cb9eb00ff6`. Replaying only the exact v2 capability results yields a rectangular 112-cell inventory with digest `acb5c801afc0dd0e2e58699346afd912fbfe9775657e3eceb588fe1af04e3101`; its fresh 560-work schedule has digest `b68ac62cf8ed9c670264655625bede3f533149136429f02505393fd22b5289f1`. Clean regeneration reproduced the plan and model byte-for-byte; 23 focused tests passed in 1.51 seconds, Ruff and `git diff --check` passed. |
| 2026-09-19 | `traf-94-v2-model` | V3 pre-measurement metadata correction | The first supervisor launch rejected work index 0 before creating a work directory or invoking the native timing probe because `source_commit` incorrectly named the SimLLM preregistration commit rather than the pinned NCCL source commit. The external status recorded zero completed work and the work root contained no files, so no v3 timing value was opened. Corrected `source_commit` to probe identity `7b83616df3ae082a1f32bb74c27458bfe8153a13`, regenerated every dependent artifact and identity listed above, reran qualification and schedule construction, and required a second remote preregistration before retry. |
| 2026-09-19 | `traf-94-v2-model` | V3 100-work milestone | The corrected preregistration was pushed before timing. The authoritative status and independent `COMPLETE.json` count reached 103 of 560 fresh, hash-complete attempts at the checkpoint, with zero `FAILED.json` and zero `CONTAMINATED.json` markers. The serial replication schedule continues on physical GPUs 0,1,2,5. |
| 2026-09-19 | `traf-94-v2-model` | V3 200-work milestone | The authoritative status and independent `COMPLETE.json` count reached 202 of 560 fresh, hash-complete attempts at the checkpoint. The 100-to-200 interval completed with zero `FAILED.json` and zero `CONTAMINATED.json` markers; the serial replication schedule continues on physical GPUs 0,1,2,5. |
| 2026-09-19 | `traf-94-v2-model` | V3 300-work milestone | The authoritative status and independent `COMPLETE.json` count reached 304 of 560 fresh, hash-complete attempts at the checkpoint. The 200-to-300 interval completed with zero `FAILED.json` and zero `CONTAMINATED.json` markers; the serial replication schedule continues on physical GPUs 0,1,2,5. |
| 2026-09-19 | `traf-94-v2-model` | V3 work-376 contention rejection and rank-derived device guard | After 376 accepted attempts, the immediate post-run guard rejected work index 376 when external user `knakaji`'s `.conda/bin/python` training process appeared on selected GPU 0 (and its paired context occupied GPU 1). The rejected attempt and marker are preserved under `quarantine/postcheck-device-transition-20260919/work-000376-attempt1` and removed from active evidence. Because the qualified v3 inventory has maximum rank three and only GPUs 2,4,5 remained idle, the supervisor's old hard-coded four-device prerequisite was replaced by a validated `max(requested.ranks)` requirement. This executor correction changes no frozen request, prediction, schedule, timer, or acceptance rule; five focused supervisor tests pass. Work index 376 must rerun after a fresh stable-idle gate on GPUs 2,4,5. |
| 2026-09-19 | `traf-94-v2-model` | V3 400-work milestone | External work later occupied GPUs 2 and 4, so the resumable supervisor remained at its pre-run idle gate until GPUs 5,6,7 became an idle trio. Work index 376 then reran cleanly, and the authoritative status plus independent `COMPLETE.json` count reached 401 of 560 accepted attempts at the checkpoint. Active work contains zero `FAILED.json` and zero `CONTAMINATED.json` markers; the single rejected attempt remains only in quarantine. |
| 2026-09-19 | `traf-94-v2-model` | V3 500-work milestone | The authoritative status and independent `COMPLETE.json` count reached 503 of 560 accepted attempts at the checkpoint. The 400-to-500 interval completed on GPUs 5,6,7 with zero `FAILED.json` and zero active `CONTAMINATED.json` markers; the single rejected work-376 attempt remains preserved only in quarantine. |
| 2026-09-19 | `traf-94-v2-model` | V3 execution, validation, and locked score complete | The resumed supervisor completed all 560 frozen work items. Active evidence has exactly 560 work directories and 560 `COMPLETE.json` markers, zero `FAILED.json`, and zero `CONTAMINATED.json`; the one rejected work-376 attempt remains only in quarantine. Collection produced 168,000 fresh rows with SHA-256 `e8da409b02c8b919179b48829b63bda6e1585fa576f59a2b941d10e661947b88`; validation found zero failures and zero void scopes. Of 816 interventions, 195 resolve, 184 satisfy the unchanged bound, and 11 do not. The immutable outcome is `FAIL`, score digest `212a925888f9260b4ac8452ad644267a620fe63ef78fbaec92a492cbe5ff34a2`, compact result digest `92596f09a941baa6c63976d62b22d005c49eca458310ed5163d5b5f6acf5263e`. |

## Commands

Commands and exact results will be added here as implementation gates run. Raw
hardware captures must remain outside Git; only compact summaries and their
content-hash manifest belong in the repository.

TRAF-94 planning/audit commands run on 2026-09-11:

```bash
python examples/nccl_primitive_identification_v1/audit_environment.py \
  --extension examples/nccl_primitive_identification_v1/h100-extension.json \
  --source /home/siyshen/workspace/nccl \
  --library /lib/x86_64-linux-gnu/libnccl.so \
  --output /tmp/traf94-h100-environment-audit.json

python examples/nccl_primitive_identification_v1/run_study.py plan \
  --architecture-extension examples/nccl_primitive_identification_v1/h100-extension.json \
  --output /tmp/traf94-h100-planned.json

python examples/nccl_primitive_identification_v1/run_study.py capability-plan \
  --inventory /tmp/traf94-h100-planned.json \
  --output /tmp/traf94-h100-capabilities.jsonl
```

Those commands document the superseded 2026-09-11 planning pass. The completed
2026-09-16 source-probe pass expanded 848 cells into 388 support-sensitive
capability requests and froze 768 qualified cells. Its compact record is
`examples/nccl_primitive_identification_v1/h100-capability-freeze.json`; raw
requests, native stdout/stderr, qualified inventory, schedule, and observation
rows remain in the external capture area named by their SHA-256 hashes.

The H100 execution work is complete and published. The v1 additive candidate,
v2 sparse interaction tensor, and v3 exact-control replication table all remain
immutable `FAIL` results under their prospectively frozen rules. This closes
the runner/campaign/publication work without claiming a calibrated point model:
the evidence instead shows that a follow-up must model cross-run uncertainty or
execution state and obtain another independent holdout.

Host-local evidence is retained under
`/home/siyshen/workspace/traf94-campaign-final5`,
`/home/siyshen/workspace/traf94-v2-campaign`, and
`/home/siyshen/workspace/traf94-v3-campaign`. These paths are operator handoff
locators, not portable artifacts. The committed compact results bind each
qualified inventory, schedule, merged-artifact manifest, observation capture,
validation report, and complete score by digest or file SHA-256.

## Handoff notes for future engineers and agents

- The retained packet engine is the single clock owner. Request-visibility
  callbacks may enqueue GPU work on that calendar but must never advance it
  recursively.
- A read response is intentionally ineligible until
  `release_read_response(extent_id)` is called. The call is one-shot and is
  legal only after the request is consumer-visible. Do not replace this gate
  with a duration added to packet transport: doing so loses owner-memory
  contention and causal attribution.
- For a Simple read, the resident block belongs to the requester while the
  memory cursor belongs to the source/buffer owner. `memory_rank` represents
  precisely this split; it does not imply a resident block on the owner.
- `buffered_read` is communicator placement, not a fourth protocol. LL and
  LL128 still execute writes on that communicator; only Simple payload motion
  changes direction.
- The critical-path packet-only reporter cannot describe an external GPU
  service gate and therefore rejects this path before admission. Extend its
  evidence model before relaxing that guard.
- TRAF-94 must not turn an unqualified H100 run into a result merely because
  this checkout happens to be on H100 hardware. Freeze the realized source,
  build, device, and expanded-cell inventory first.
